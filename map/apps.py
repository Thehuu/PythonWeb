from django.apps import AppConfig


class MapConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'map'
    verbose_name = 'các điểm tai nạn, thiên tai, cháy, nổ, cứu nạn, cứu hộ'